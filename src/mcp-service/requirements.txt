# mcp-service — service độc lập (không dùng chung core_engine rag-worker).
# Ghép với rag-worker CHỈ qua Qdrant.
mcp>=1.2.0
qdrant-client>=1.7.0
openai>=1.30.0
PyYAML>=6.0
pydantic>=2.0
# DB — hr_query tool (schema hr_mock trong mcp_db)
sqlalchemy>=2.0
alembic>=1.13
psycopg[binary]>=3.1
# Test
pytest>=8.0
pytest-asyncio>=0.23
