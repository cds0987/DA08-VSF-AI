# Query Service v2 - Guarded Tool Routing & MCP Client

> **Note:** As of the latest update, `query-service` supports **two agent modes**:
> - `AGENT_MODE=guarded` (default): Guarded Tool Routing (this document describes v2)
> - `AGENT_MODE=react`: True Agent with ReAct pattern (see v3 section below)

## Mục tiêu

Phiên bản v2 đưa `query-service` về đúng hướng kiến trúc hệ thống: Query Service là **LLM orchestration + MCP client**, để LLM chọn tool `rag_search` hoặc `hr_query`, còn backend giữ các chốt an toàn bằng code.

So với bản v1 đã push trên git (`origin/dungpq/query-service`, commit `79301a5`), v1 route câu hỏi bằng `_detect_intent()` hard-code trong `QueryOrchestrationUseCase`. Cách đó nhanh và dễ test, nhưng lệch kiến trúc vì Query Service tự phân loại intent bằng keyword thay vì để agent/tool-calling chọn tool từ MCP.

V2 không dùng thêm một LLM thứ hai để validate. Validation là deterministic guard:

- Chỉ cho phép tool trong allowlist: `rag_search`, `hr_query`.
- `hr_query.intent` chỉ nhận `leave_balance`, `leave_requests`, `payroll`.
- `user_id` luôn lấy từ authenticated user/JWT, không nhận từ LLM.
- `document_ids` luôn lấy từ ACL projection, không nhận từ LLM.
- `top_k` cố định/clamp ở backend.
- Tool lạ, intent sai, decision lỗi hoặc tool unavailable đều fallback về `rag_search`.

## Luồng xử lý mới

1. `POST /query` nhận `question` và `user_id`, xác thực user như cũ.
2. `QueryOrchestrationUseCase` lưu user message và lấy recent conversation context.
3. Câu identity như “Bạn là ai?” vẫn trả lời trực tiếp bằng canned response, không gọi tool.
4. Với câu hỏi còn lại, Query Service gọi `ToolDecisionClient.choose_tool()` để LLM/mock chọn tool.
5. Backend normalize decision:
   - `hr_query` hợp lệ -> gọi MCP tool với `user_id` từ auth và `intent` đã validate.
   - Mọi decision khác -> đi `rag_search`.
6. Với `rag_search`, backend lấy `allowed_doc_ids` từ `DocumentAccessRepository`, check semantic cache, rồi gọi MCP tool với `query`, `document_ids`, `top_k=RAG_RESULT_LIMIT` (default 3).
7. Kết quả RAG vẫn bị lọc bằng `RAG_SCORE_THRESHOLD`; score thấp thì fallback “Không tìm thấy thông tin trong tài liệu nội bộ.”
8. Response SSE giữ nguyên format `token` và event cuối `done/sources/session_id`.

## Thay đổi so với v1 đã push

### 1. Orchestration không còn tự route bằng keyword

V1 dùng `_detect_intent()` trong `orchestration.py` để trả về `identity`, `hr:*` hoặc `rag`.

V2 bỏ `_detect_intent()` khỏi flow chính. `orchestration.py` có `_choose_tool()`:

- gọi `mcp_client.list_tools()`;
- gọi `tool_decision_client.choose_tool()`;
- normalize decision bằng `normalize_tool_decision()`;
- fallback về `rag_search` nếu decision không hợp lệ.

### 2. Thêm contract tool decision

File mới `app/application/tool_decision.py` định nghĩa:

- `ToolDecision`;
- `VALID_TOOL_NAMES`;
- `VALID_HR_INTENTS`;
- `normalize_tool_decision()`.

Đây là lớp guard chính để không tin raw output từ LLM.

### 3. Thêm ToolDecisionClient

File mới `app/infrastructure/external/tool_decision_client.py` có:

- `MockToolDecisionClient`: dùng cho test/dev, có thể force decision trong test.
- `OpenAIToolDecisionClient`: dùng OpenAI để trả JSON chọn tool.

Trong production path, LLM chỉ chọn tool và HR intent. LLM không được sinh `user_id`, `document_ids`, `top_k` hay dữ liệu phân quyền.

### 4. Thêm MCP Streamable HTTP SDK adapter

V1 chỉ có `MockMCPClient`.

V2 thêm `MCPStreamableHttpClient` trong `app/infrastructure/external/mcp_client.py`:

- official MCP Python SDK `streamable_http_client(...)` + `ClientSession.initialize()`;
- `list_tools()` để lấy danh sách tool;
- `call_tool()` với `rag_search`;
- `call_tool()` với `hr_query`;
- map MCP result về DTO `SearchResult` và `HrQueryResult`.

`MCP_MODE=mock` vẫn dùng mock. `MCP_MODE=real` dùng SDK client gọi `MCP_SERVICE_URL/mcp`. Legacy `MCP_MODE=mcp` được chấp nhận như alias cho `real` trong giai đoạn migration.

### 5. Dependency injection đổi theo config

`interfaces/api/dependencies.py` hiện chọn:

- MCP client: `MockMCPClient` hoặc `MCPStreamableHttpClient`;
- tool decision client: `MockToolDecisionClient` hoặc `OpenAIToolDecisionClient`.

`reset_state_for_tests()` cũng được chỉnh để reset an toàn với cả mock client và client thật.

### 6. Infrastructure NATS/query_db adapters

V2 infrastructure update giữ đúng ranh giới trong docs:

- Query Service là consumer của `doc.access` và `notify.doc_new`.
- Query Service không subscribe `doc.ingest`, không request `rag.search`, và không gọi RAG Worker trực tiếp.
- Khi `NATS_MODE=mock`, hệ thống giữ `InMemoryDocumentAccessRepository` và `InMemoryNotificationRepository` cho dev/test offline.
- Khi `NATS_MODE=nats`, FastAPI lifespan start `NatsSubscriberManager`:
  - `doc.access` -> upsert/delete `query_svc.document_access`.
  - `notify.doc_new` -> lưu notification và push SSE `/notifications`.
- Postgres adapters dùng `DATABASE_URL` tới `query_db`; NATS message chỉ ack sau khi handler xử lý xong.
- Payload hỏng được ack để tránh redelivery vô hạn; lỗi tạm thời khi ghi DB/push notification sẽ `nak` để JetStream retry.

### 7. Intent classifier cũ chuyển thành legacy/dev

Các file `intent_classifier.py`, `intent_ai_client.py` và config `INTENT_*` hiện không còn là router production chính. Chúng được giữ như legacy/dev support và test riêng, nhưng flow chính của `QueryOrchestrationUseCase` đã chuyển sang guarded tool decision.

## Cấu hình liên quan

```env
# mock: dùng MockMCPClient cho dev/test
# real: gọi MCP Streamable HTTP bằng official Python SDK ở MCP_SERVICE_URL(/mcp)
# mcp: legacy alias cho real
MCP_MODE=mock
MCP_SERVICE_URL=http://localhost:8003
MCP_TIMEOUT_SECONDS=10

# mock: không start NATS subscriber, dùng in-memory repos
# nats: subscribe doc.access + notify.doc_new, ghi vào query_db
NATS_MODE=mock
NATS_URL=nats://localhost:4222
NATS_JETSTREAM_ENABLED=true

# LLM_MODE=mock -> dùng MockToolDecisionClient
# LLM_MODE=openai + OPENAI_API_KEY -> dùng OpenAIToolDecisionClient
LLM_MODE=openai
OPENAI_API_KEY=
OPENAI_LLM_MODEL=gpt-4o-mini
OPENAI_TIMEOUT_SECONDS=30

# Legacy/dev only, không phải production router chính của v2
INTENT_CLASSIFIER_MODE=hybrid
INTENT_RULE_CONFIDENCE_THRESHOLD=0.90
INTENT_EMBEDDING_CONFIDENCE_THRESHOLD=0.78
INTENT_EMBEDDING_MARGIN=0.08
INTENT_LLM_CONFIDENCE_THRESHOLD=0.70
INTENT_LLM_MODEL=gpt-4o-mini
INTENT_LLM_TIMEOUT_SECONDS=5
```

## File chính thay đổi/thêm mới

Tracked changes so với `origin/dungpq/query-service`:

- `app/application/use_cases/query/orchestration.py`: bỏ route bằng `_detect_intent()`, thêm guarded `_choose_tool()` và tách `_handle_rag()`.
- `app/application/ports.py`: thêm `ToolDecisionClient` protocol.
- `app/infrastructure/external/mcp_client.py`: thêm `MCPStreamableHttpClient` và mapper result.
- `app/interfaces/api/dependencies.py`: wire MCP client/tool decision client theo config.
- `app/infrastructure/config.py` và `.env.example`: thêm/ghi chú config legacy intent và MCP mode.
- `app/infrastructure/messaging/nats_events.py`: parse/handle `doc.access` và `notify.doc_new`.
- `app/infrastructure/messaging/nats_subscriber.py`: NATS JetStream subscriber manager cho Query Service.
- `app/infrastructure/db/postgres_document_access_repo.py`: production adapter cho `query_svc.document_access`.
- `app/infrastructure/db/postgres_notification_repo.py`: production adapter cho `query_svc.notifications`.
- `app/interfaces/api/schemas/query.py`: public source field theo GCP docs la `source_gcs_uri`, khong phai `source_s3_uri`.
- `tests/test_api.py`: thêm regression test cho guarded routing.

Untracked/new files trong v2 working tree:

- `app/application/tool_decision.py`
- `app/infrastructure/external/tool_decision_client.py`
- `tests/test_mcp_streamable_client.py`
- `app/application/intent_classifier.py` và `app/infrastructure/external/intent_ai_client.py` (legacy/dev classifier)
- `tests/test_intent_classifier.py`

## Kiểm thử

Đã bổ sung/giữ test cho:

- HR decision hợp lệ gọi `hr_query` và backend inject `user_id` từ authenticated user.
- LLM/mock decision cố truyền `user_id` khác bị bỏ qua.
- RAG decision cố truyền `document_ids`/`top_k` bị bỏ qua; backend dùng ACL và `RAG_RESULT_LIMIT`.
- Tool lạ fallback về `rag_search`.
- HR intent lạ fallback về `rag_search`.
- User thường không thấy tài liệu top secret.
- Semantic cache không leak source giữa các scope ACL.
- MCP Streamable HTTP SDK client map được `list_tools`, `rag_search`, `hr_query`.
- NATS infrastructure test cover `doc.access` upsert/delete/stale event, `notify.doc_new` dedup, subscriber ack/nak, và dependency selection Postgres khi `NATS_MODE=nats`.
- Query API regression test đảm bảo source trả về `source_gcs_uri` và không còn `source_s3_uri`.
- Legacy `HybridIntentClassifier` vẫn có test riêng, nhưng không đại diện cho production routing chính của v2.

Lệnh verify gần nhất:

```powershell
cd src/query-service
.\.venv\Scripts\python -m pytest tests -q
```

Kết quả hiện tại: `57 passed`.

## Giới hạn còn lại

- True ACL-before-Qdrant cần follow-up ở `mcp-service`/search layer vì query-service chỉ có thể post-filter khi `document_ids` chưa được honored trước truy vấn vector.
- Chưa có observability riêng cho tool decision: nên log decision, fallback reason, selected tool và latency vào Langfuse khi hoàn thiện observability.
- Chưa có confidence score cho tool decision; hiện policy là fallback deterministic khi schema/tool/intent không hợp lệ.
- `HybridIntentClassifier` còn tồn tại trong code như legacy/dev path. Nếu không dùng nữa, nên có task cleanup riêng để xóa file/config/test liên quan và tránh nhầm lẫn.

---

## v3 — True Agent with ReAct Pattern (AGENT_MODE=react)

### Tổng quan

v3 nâng cấp `query-service` từ Guarded Tool Routing thành **True Agent** sử dụng ReAct pattern. LLM điều khiển hoàn toàn việc quyết định có gọi tool hay không, gọi tool nào, với arguments gì. Backend chỉ enforce ACL/auth boundaries.

### Sự khác biệt cốt lõi

| Aspect | v2 Guarded Routing | v3 True Agent |
|--------|--------------------|---------------|
| LLM calls | Cố định 2 | Dynamic 1-N |
| Tool execution | Backend quyết định | LLM quyết định |
| Loop control | Không có | Có - ReAct cycle |
| Replan on failure | Không | Có |
| Reasoning trace | Không | Có |
| Max iterations | N/A | Có (default: 3) |

### Files mới trong v3

| File | Mô tả |
|------|--------|
| `app/application/agent_loop.py` | `QueryAgentLoop` - core ReAct loop |
| `app/application/tools.py` | Tool definitions + JSON schemas |
| `app/infrastructure/external/agent_llm_client.py` | `OpenAIAgentLLMClient` + `MockAgentLLMClient` |

### Files thay đổi trong v3

| File | Thay đổi |
|------|---------|
| `app/domain/outcome.py` | Thêm `AgentState` enum |
| `app/application/ports.py` | Thêm `AgentLLMClient`, `ToolCall`, `ToolResult`, `ReActMessage`, `ReActOutput` protocols |
| `app/infrastructure/config.py` | Thêm `AGENT_MODE`, `AGENT_MAX_ITERATIONS`; production guard bắt buộc `AGENT_MODE=react` |
| `app/application/use_cases/query/orchestration.py` | Delegate to `QueryAgentLoop` khi có agent_loop |
| `app/interfaces/api/dependencies.py` | Wire `AgentLoop`, `AgentLLMClient` qua DI |
| `docs/architecture.md` | Thêm section Query Service Agent Architecture |
| `src/query-service/Docs/Version/v2.md` | Thêm section v3 (document này) |

### Cấu hình v3

```env
# guarded: legacy Guarded Tool Routing (default, backward compatible)
# react: True Agent với ReAct pattern
AGENT_MODE=react
AGENT_MAX_ITERATIONS=3
```

### ReAct Loop Flow

```
QueryAgentLoop.run()
  │
  ├─ Iteration 1:
  │    ├─ LLM.react_step() → THOUGHT: "Cần tìm chính sách nghỉ phép"
  │    ├─ LLM.react_step() → ACTION: tool_call(rag_search, {query: "chính sách nghỉ phép"})
  │    ├─ Backend ACL Guard: inject document_ids từ ACL
  │    ├─ MCP Client: execute rag_search
  │    └─ LLM.react_step() → OBSERVE: nhận kết quả, quyết định đủ info
  │
  ├─ Iteration 2 (nếu cần):
  │    ├─ LLM.react_step() → ACTION: tool_call(hr_query, {intent: "leave_balance"})
  │    ├─ Backend Guard: inject user_id từ auth
  │    └─ LLM.react_step() → OBSERVE: nhận kết quả HR
  │
  └─ Final:
       ├─ LLM.react_step() → FINAL_ANSWER: stream via SSE
       └─ Done event: sources, session_id, iterations
```

### ACL Guard trong v3

Backend guard chặt chẽ hơn v2:

```python
# _execute_with_guard() trong agent_loop.py
if call.tool_name == "rag_search":
    allowed_doc_ids = await self._doc_access.get_allowed_doc_ids(...)
    # LLM KHÔNG thể override document_ids
    results = await self._mcp.rag_search(query=..., document_ids=allowed_doc_ids, ...)

if call.tool_name == "hr_query":
    # user_id luôn từ auth - LLM không bao giờ được set
    result = await self._mcp.hr_query(user_id=user.id, intent=...)
```

### Backward Compatibility

- `AGENT_MODE=guarded` (default) giữ nguyên behavior v2 - 100% backward compatible
- Không break existing API
- Feature flag cho phép migrate từ từ: test với subset users trước

### Giới hạn

- Max iterations (default: 3) ngăn infinite loop nhưng có thể không đủ cho complex multi-step queries
- LLM reasoning cost cao hơn guarded routing
- OpenAI API key bắt buộc cho `AGENT_MODE=react` (fallback `MockAgentLLMClient` không có OpenAI key)
- Production bắt buộc `AGENT_MODE=react` (config validation)
