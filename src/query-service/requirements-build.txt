# Build-time ONLY deps cho stage price-builder.
# Trước đây cần huggingface-hub + pyarrow để tải + parse parquet (pyarrow ~40-60MB ->
# build chậm). Nay fetch qua HF datasets-server REST (JSON) bằng urllib THUẦN STDLIB
# -> KHÔNG còn dep build nào. Giữ file để Dockerfile `pip install -r` chạy no-op.
# Xem app/infrastructure/observability/price_catalog.py::fetch_prices_from_hf.
