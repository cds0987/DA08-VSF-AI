"""Self-test offline cho haystack_interface (assert-based, không cần pytest).

    python -m haystack_interface.tests.selftest           # bất biến core
    python -m haystack_interface.tests.selftest_provider  # AI gateway: SDK wiring + e2e
"""
