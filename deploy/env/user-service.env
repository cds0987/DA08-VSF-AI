# ============================================================================
# user-service.env — CHỈ biến RIÊNG. Biến chung ở common.env (JWT_SECRET_KEY,
# CORS_ORIGINS, REDIS_URL).
# ============================================================================

# DB (Cloud SQL) — driver psycopg v3 (khớp VM). Config đọc USER_SERVICE_DATABASE_URL
# trước, fallback DATABASE_URL (VM dùng DATABASE_URL); user-migrate (alembic env.py)
# cũng đọc cả hai -> migrate + runtime cùng 1 DB.
DATABASE_URL=postgresql+psycopg://postgres:***REDACTED-DB-PW***@34.87.63.152:5432/user_db

ACCESS_TOKEN_TTL_MINUTES=10080
REFRESH_TOKEN_TTL_DAYS=7
FAILED_LOGIN_THRESHOLD=5
LOCKOUT_MINUTES=15
