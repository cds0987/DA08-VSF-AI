.claude/
proposal/
.env
# Private repo nội bộ: env thật của các service được SHARE cho team (track trong git).
.env-backup-*/
.venv/
venv/
__pycache__/
.pytest_cache/
.pytest-run-*/
.pytest-tmp*/
.ruff_cache/
pytest-cache-files-*/
*.py[cod]
*.db
query-body.json
feedback-body.json
notify-body.json
src/mcp-service/tests/.tmp/
src\rag-worker\deploy\standalone\docker-compose.standalone.yml
# Chỉ KHÔNG push file khởi tạo DB (schema tạo tay theo docs); phần còn lại push bình thường.
infra/localtest/init-db.sql

# deploy/env/*.env: COMMIT thẳng vào git (repo private) -> env đi cùng code, deploy
# `git reset --hard` đồng bộ luôn. KHÔNG ignore. Nguồn sự thật DUY NHẤT = git.
.codegraph