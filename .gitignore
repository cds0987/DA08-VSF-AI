.claude/
proposal/
.env
# Private repo nội bộ: env thật của các service được SHARE cho team (track trong git).
.env-backup-*/
.venv/
venv/
__pycache__/
.pytest_cache/
.pytest-run-*/
.pytest-tmp*/
.ruff_cache/
pytest-cache-files-*/
*.py[cod]
*.db
query-body.json
feedback-body.json
notify-body.json
# SA key JSON (native GCS cho document-service): KHÔNG commit — đặt trực tiếp trên
# VM (deploy-develop.yml). Khác env: đây là private key, sinh lại được, không share git.
deploy/secrets/
src/mcp-service/tests/.tmp/
src\rag-worker\deploy\standalone\docker-compose.standalone.yml
# Chỉ KHÔNG push file khởi tạo DB (schema tạo tay theo docs); phần còn lại push bình thường.
infra/localtest/init-db.sql

# deploy/env/*.env: file CONFIG (không secret) commit thẳng -> env đi cùng code.
# ⚠ NGOẠI LỆ: secret.env chứa SECRET THẬT -> KHÔNG BAO GIỜ commit (public repo).
deploy/env/secret.env
# .htpasswd Basic Auth subdomain Langfuse: render từ GitHub Secret mỗi deploy -> KHÔNG commit.
deploy/nginx/.htpasswd
deploy/nginx/.htpasswd-qdrant
.codegraph
local-stack/
# .pw-test/: harness test cục bộ (Playwright) — chứa creds Langfuse/login plaintext + ảnh/log
# dung lượng lớn. KHÔNG commit (tránh lộ mật khẩu admin + Basic Auth).
.pw-test/