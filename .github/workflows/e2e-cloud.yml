# E2e CLOUD THẬT: document-service -> NATS -> rag-worker -> Qdrant -> mcp search.
# Chạy trên Docker của GitHub runner (KHÔNG dùng docker local). Postgres + NATS là
# container trên runner; GCS + Qdrant Cloud + OpenAI là dịch vụ THẬT.
# Sau mỗi lần chạy (kể cả fail) -> DỌN SẠCH object đã upload trên GCS + xóa mọi
# collection trên Qdrant Cloud.
#
# Yêu cầu GitHub Secrets:
#   OPENAI_API_KEY, QDRANT_URL (có :6333), QDRANT_API_KEY,
#   GCS_HMAC_KEY, GCS_HMAC_SECRET, JWT_SECRET_KEY
# (GCS bucket cố định: vsf-rag-chatbot-docs-dev)

name: e2e-cloud (ingest + search trên GCP/Qdrant thật)

on:
  workflow_dispatch:
  push:
    branches: [nguyendev]
  pull_request:
    branches: [develop]
    paths:
      - "src/rag-worker/**"
      - "src/mcp-service/**"
      - "src/document-service/**"
      - "docker-compose.localtest.yml"
      - "infra/localtest/ci_e2e.py"
      - ".github/workflows/e2e-cloud.yml"

# Hạ tầng cloud DÙNG CHUNG (1 bucket + 1 cluster Qdrant) -> không chạy song song,
# tránh 2 run ghi/xóa đè nhau.
concurrency:
  group: e2e-cloud-shared
  cancel-in-progress: false

env:
  GCS_BUCKET: vsf-rag-chatbot-docs-dev
  # Compose interpolation đọc các biến này:
  OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
  QDRANT_URL: ${{ secrets.QDRANT_URL }}
  QDRANT_API_KEY: ${{ secrets.QDRANT_API_KEY }}
  GCS_HMAC_KEY: ${{ secrets.GCS_HMAC_KEY }}
  GCS_HMAC_SECRET: ${{ secrets.GCS_HMAC_SECRET }}
  JWT_SECRET_KEY: ${{ secrets.JWT_SECRET_KEY }}
  # Metadata DB: compose default = Cloud SQL (production). CI OVERRIDE -> postgres
  # container local (isolated, runner không tới được Cloud SQL + không đụng prod).
  RAG_DATABASE_URL: postgresql+psycopg://postgres:postgres@postgres:5432/rag_db
  DOC_DATABASE_URL: postgresql+asyncpg://postgres:postgres@postgres:5432/doc_db

jobs:
  e2e-cloud:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Install test client deps
        # boto3/requests/mcp cho ci_e2e; pydantic*/httpx để chạy CLIENT THẬT của
        # query-service (MCPStreamableHttpClient + Settings) ở bước query-search.
        run: python -m pip install --upgrade pip boto3 requests mcp pydantic pydantic-settings httpx

      # init-db.sql KHÔNG được commit (gitignored) -> sinh ở runtime cho compose mount.
      - name: Generate infra/localtest/init-db.sql
        run: |
          mkdir -p infra/localtest
          cat > infra/localtest/init-db.sql <<'SQL'
          CREATE DATABASE doc_db;
          CREATE DATABASE rag_db;
          \connect doc_db
          CREATE SCHEMA IF NOT EXISTS doc_svc;
          CREATE TABLE IF NOT EXISTS doc_svc.documents (
              id UUID PRIMARY KEY,
              name VARCHAR(500) NOT NULL,
              file_type VARCHAR(20) NOT NULL,
              gcs_key VARCHAR(1000) NOT NULL,
              status VARCHAR(20) NOT NULL DEFAULT 'queued',
              uploaded_by UUID NOT NULL,
              classification VARCHAR(20) NOT NULL DEFAULT 'internal',
              allowed_departments TEXT[] NOT NULL DEFAULT '{}',
              allowed_user_ids TEXT[] NOT NULL DEFAULT '{}',
              chunk_count INTEGER NOT NULL DEFAULT 0,
              error_message TEXT,
              created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
              updated_at TIMESTAMPTZ NOT NULL DEFAULT now(),
              deleted_at TIMESTAMPTZ
          );
          CREATE INDEX IF NOT EXISTS ix_doc_status ON doc_svc.documents (status);
          CREATE INDEX IF NOT EXISTS ix_doc_uploaded_by ON doc_svc.documents (uploaded_by);
          CREATE TABLE IF NOT EXISTS doc_svc.audit_logs (
              id UUID PRIMARY KEY,
              actor_id UUID NOT NULL,
              actor_role VARCHAR(50) NOT NULL,
              action VARCHAR(100) NOT NULL,
              resource_type VARCHAR(100),
              resource_id UUID,
              detail JSONB,
              ip_address VARCHAR(45),
              created_at TIMESTAMPTZ NOT NULL DEFAULT now()
          );
          CREATE INDEX IF NOT EXISTS ix_audit_actor ON doc_svc.audit_logs (actor_id);
          SQL

      - name: Build + start stack (postgres+nats local; GCS+Qdrant cloud)
        run: docker compose -f docker-compose.localtest.yml up -d --build

      - name: Wait for rag-worker /readyz
        run: |
          for i in $(seq 1 60); do
            if curl -fsS http://localhost:8000/readyz >/dev/null 2>&1; then
              echo "rag-worker ready"; exit 0
            fi
            sleep 3
          done
          echo "rag-worker not ready" >&2
          docker compose -f docker-compose.localtest.yml logs rag-worker || true
          exit 1

      - name: Upload validation corpus via document-service
        env:
          S3_ENDPOINT_URL: https://storage.googleapis.com
          S3_BUCKET: ${{ env.GCS_BUCKET }}
          S3_ACCESS_KEY_ID: ${{ secrets.GCS_HMAC_KEY }}
          S3_SECRET_ACCESS_KEY: ${{ secrets.GCS_HMAC_SECRET }}
          S3_REGION: auto
        run: python infra/localtest/ci_e2e.py upload

      - name: Verify ingest (poll Qdrant points)
        run: python infra/localtest/ci_e2e.py verify

      # mcp verify-contract fail-closed: chỉ pass SAU khi data collection đã có
      # (tạo ở lần ingest đầu) -> start lại mcp sau khi verify.
      - name: (Re)start mcp-service after collection exists
        run: |
          docker compose -f docker-compose.localtest.yml up -d mcp-service
          for i in $(seq 1 30); do
            if docker compose -f docker-compose.localtest.yml logs mcp-service 2>&1 | grep -q "Uvicorn running"; then
              echo "mcp up"; exit 0
            fi
            sleep 2
          done
          echo "mcp not up" >&2
          docker compose -f docker-compose.localtest.yml logs mcp-service || true
          exit 1

      - name: Search golden queries via MCP HTTP
        run: python infra/localtest/ci_e2e.py search

      # Mắt xích query-service: dùng CHÍNH client production của query-service
      # (MCPStreamableHttpClient) gọi rag_search của mcp -> nghiệm thu kết nối
      # query-service -> mcp-service qua search trên corpus vừa ingest.
      - name: Query-service -> MCP search connectivity (real query-service client)
        env:
          MCP_SERVICE_URL: http://localhost:8003
        run: python infra/localtest/ci_e2e.py query-search

      - name: Stack logs on failure
        if: failure()
        run: docker compose -f docker-compose.localtest.yml logs --no-color | tail -300 || true

      # DỌN SẠCH luôn chạy (kể cả fail): xóa object đã upload trên GCS + mọi collection Qdrant.
      - name: Cleanup GCS + Qdrant
        if: always()
        env:
          S3_ENDPOINT_URL: https://storage.googleapis.com
          S3_BUCKET: ${{ env.GCS_BUCKET }}
          S3_ACCESS_KEY_ID: ${{ secrets.GCS_HMAC_KEY }}
          S3_SECRET_ACCESS_KEY: ${{ secrets.GCS_HMAC_SECRET }}
          S3_REGION: auto
        run: python infra/localtest/ci_e2e.py cleanup

      - name: Tear down stack
        if: always()
        run: docker compose -f docker-compose.localtest.yml down -v || true
